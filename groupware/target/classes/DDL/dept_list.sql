CREATE TABLE DEPT_LIST(
  DEPT_CODE VARCHAR2(10) PRIMARY KEY,
  DEPT_NAME VARCHAR2(50) NOT NULL UNIQUE,
  MGR_EMP_CODE VARCHAR2(10) NOT NULL,
  INP_DATE DATE DEFAULT SYSDATE,
  UPDATE_DATE DATE DEFAULT SYSDATE
);