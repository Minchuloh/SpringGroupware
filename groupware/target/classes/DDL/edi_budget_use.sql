CREATE TABLE EDI_BUDGET_USE(
  EDI_CODE VARCHAR2(20),  
  USE_BUDGET NUMBER(10, 0) NOT NULL,
  REFUND_DATE DATE NOT NULL,
  BANK_NAME VARCHAR2(30) NOT NULL,
  BANK_ACCOUNT VARCHAR2(50) NOT NULL,
  BANK_OWNER VARCHAR2(6),
  INP_DATE DATE DEFAULT SYSDATE,
  UPDATE_DATE DATE DEFAULT SYSDATE,
  CONSTRAINT PK_EDI_BUDGET_USE PRIMARY KEY(EDI_CODE)
);